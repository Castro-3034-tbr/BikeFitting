# Carpetas que queremos anadir al repositorio
# Estas carpetas deben contener archivos que no esten en .gitignore
# y que no esten vacias.
# Si una carpeta esta vacia, Git no la anadira al repositorio.
# Para mantener la carpeta vacia, se puede anadir un archivo .gitkeep dentro de ella
# para que Git la reconozca.
# Aqui se puede anadir el nombre de la carpeta que se quiere anadir al repositorio.
# Por ejemplo, si queremos anadir la carpeta "test", podemos anadirla aqui.